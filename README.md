# InventoryManager Project

## Goal
Build a robust inventory management system using an object-oriented approach.

## Status
- **Meetings Held**: 1
- **Status**: Active

## Completed Work
- Refactored `inventory.py` to use an `Inventory` class for better state management.
- Rewrote `acceptance_tests.py` to test the new class-based implementation.

## Next Steps
- Run acceptance tests to verify all criteria are met.
- Extend project scope if tests pass (e.g., persistence, API).
